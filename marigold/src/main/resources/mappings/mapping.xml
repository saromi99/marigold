<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ShoesDAO">

	<insert id="insertShoes">
		INSERT INTO shoes VALUES( , , , , , , , , , , , , , )
	</insert>

	<update id="updateShoes">
		
	</update>

	<delete id="deleteOneShoes">
		DELETE FROM shoes WHERE article = #{article}
	</delete>
	
	<delete id="deleteAllShoes">
		DELETE FROM shoes
	</delete>
	
	
	<select id="getCount" resultType="Integer">
		SELECT count(*) FROM shoes
	</select>

	<select id="getList" resultType="shoes">
		SELECT * FROM shoes order by article
	</select>
	
	<select id="getOne" resultType="shoes">
		SELECT * FROM shoes where article = #{article}
	</select>
	
	
	<select id="searchFirstCategoryList" resultType="shoes">
		select s.*, fc.FirstCategory from shoes s, FirstCategory fc
		    where s.FirstCategory = fc.FirstCategory
		        and ((fc.FirstCategory = '%'||#{searchKeyword}||'%') or (fc.FirstCategorycon = '%'||#{searchKeyword}||'%'))
		    order by s.article;
	</select>
	
	<select id="searchSecondCategoryList" resultType="shoes">
		select s.*, sc.secondCategory from shoes s, secondCategory sc
		    where s.secondCategory = sc.secondCategory
		        and ((sc.secondCategory = '%'||#{searchKeyword}||'%') or (sc.secondCategorycon = '%'||#{searchKeyword}||'%'))
		    order by s.article;
	</select>
	
	<select id="searchThirdCategoryList" resultType="shoes">
		select s.*, tc.thirdCategory from shoes s, thirdCategory tc
		    where s.thirdCategory = tc.thirdCategory
		        and ((tc.thirdCategory = '%'||#{searchKeyword}||'%') or (tc.thirdCategorycon = '%'||#{searchKeyword}||'%'))
		    order by s.article;
	</select>
	
	<select id="searchProductNameList" resultType="shoes">
		SELECT * FROM shoes where productName = '%'||#{searchKeyword}||'%' order by article
	</select>
	
	<select id="searchSpecialDayList" resultType="shoes">
		SELECT * FROM shoes where specialDay = '%'||#{searchKeyword}||'%' order by article
	</select>
	
	<select id="searchSpecialDayList" resultType="shoes">
		SELECT * FROM shoes where specialDay = #{specialDay} order by article
	</select>

</mapper>