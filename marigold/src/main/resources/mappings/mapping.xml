<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ShoesDAO">

	<insert id="insertShoes">
		INSERT INTO boardtb VALUES((select nvl(max(seq), 0) + 1 from boardtb), #{title}, #{writer}, #{content}, sysdate, 0)
	</insert>

	<update id="updateShoes">
		UPDATE boardTb SET title = #{title}, content = #{content} WHERE seq = #{seq}
	</update>
	
	<update id="updateCount">
		UPDATE boardTb SET cnt = cnt + 1 WHERE seq = #{seq}
	</update>

	<delete id="deleteOneShoes">
		DELETE FROM boardtb WHERE seq = #{seq}
	</delete>
	
	<delete id="deleteAllShoes">
		DELETE FROM boardtb WHERE seq = #{seq}
	</delete>

</mapper>